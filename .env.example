# Root/shared local env (`.env.local`)
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:3001
SITE_URL=http://localhost:3001

# Email / Resend
RESEND_API_KEY=
RESEND_WEBHOOK_SECRET=
RESEND_FROM_EMAIL=support@fyloph.com
RESEND_FROM=
SUPPORT_INBOX_EMAIL=support@fyloph.com

# AI
AI_PROVIDER_API_KEY=

# Public Next.js vars live in `apps/web/.env.local`
# See `apps/web/.env.example` for the required `NEXT_PUBLIC_*` values.
